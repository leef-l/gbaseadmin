import { createVNode, defineComponent } from "vue";
var DropIndicator_default = /* @__PURE__ */ defineComponent((props) => {
	return () => {
		const style = {
			pointerEvents: "none",
			position: "absolute",
			right: 0,
			backgroundColor: "red",
			height: `2px`
		};
		switch (props.dropPosition) {
			case -1:
				style.top = 0;
				style.left = `${-props.dropLevelOffset * props.indent}px`;
				break;
			case 1:
				style.bottom = 0;
				style.left = `${-props.dropLevelOffset * props.indent}px`;
				break;
			case 0:
			default:
				style.bottom = 0;
				style.left = `${props.indent}px`;
				break;
		}
		return createVNode("div", { "style": style }, null);
	};
}, {
	props: {
		dropPosition: {
			required: true,
			default: void 0
		},
		dropLevelOffset: {
			type: Number,
			required: true,
			default: void 0
		},
		indent: {
			type: Number,
			required: true,
			default: void 0
		}
	},
	name: "DropIndicator",
	inheritAttrs: false
});
export { DropIndicator_default as default };
