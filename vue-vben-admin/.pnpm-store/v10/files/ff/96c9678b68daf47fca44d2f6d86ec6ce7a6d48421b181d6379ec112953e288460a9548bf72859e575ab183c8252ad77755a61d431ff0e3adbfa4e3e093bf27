import { ref } from "vue";
function useHover() {
	const startRow = ref(-1);
	const endRow = ref(-1);
	const onHover = (start, end) => {
		startRow.value = start;
		endRow.value = end;
	};
	return [
		startRow,
		endRow,
		onHover
	];
}
export { useHover as default };
