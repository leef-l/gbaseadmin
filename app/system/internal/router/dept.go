// 部门表路由注册代码片段
// 请将以下代码添加到 app/system/internal/cmd/cmd.go 的路由注册部分

/*
	// 部门表管理
	group.Group("/dept", func(group *ghttp.RouterGroup) {
		group.Middleware(service.Middleware().Auth)
		group.Bind(
			controller.Dept,
		)
	})
*/
